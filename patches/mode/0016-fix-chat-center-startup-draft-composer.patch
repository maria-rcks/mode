From d98cbb6fad90bfa09ba70595c33ac3e31c04b207 Mon Sep 17 00:00:00 2001
From: maria-rcks <maria@kuuro.net>
Date: Sun, 14 Jun 2026 02:33:24 -0400
Subject: [PATCH 16/58] fix(chat): center startup draft composer

---
 apps/web/src/composerDraftStore.test.ts      | 17 +++++++++++++
 apps/web/src/hooks/useHandleNewThread.ts     | 10 ++++++++
 apps/web/src/lib/chatThreadActions.test.ts   | 25 ++++++++++++++++++++
 apps/web/src/lib/chatThreadActions.ts        |  3 +++
 apps/web/src/routes/_chat.draft.$draftId.tsx | 11 +++++++++
 apps/web/src/routes/_chat.index.tsx          |  1 +
 6 files changed, 67 insertions(+)

diff --git a/apps/web/src/composerDraftStore.test.ts b/apps/web/src/composerDraftStore.test.ts
index 3218b560..b7c6466b 100644
--- a/apps/web/src/composerDraftStore.test.ts
+++ b/apps/web/src/composerDraftStore.test.ts
@@ -1000,6 +1000,23 @@ describe("composerDraftStore project draft thread mapping", () => {
     });
   });
 
+  it("removes the old logical project mapping when moving a draft to another logical project", () => {
+    const store = useComposerDraftStore.getState();
+    store.setProjectDraftThreadId(projectRef, draftId, { threadId });
+
+    store.setLogicalProjectDraftThreadId("logical-b", otherProjectRef, draftId, {
+      threadId,
+    });
+
+    expect(store.getDraftSessionByLogicalProjectKey(scopedProjectKey(projectRef))).toBeNull();
+    expect(store.getDraftSessionByLogicalProjectKey("logical-b")).toMatchObject({
+      environmentId: TEST_ENVIRONMENT_ID,
+      projectId: otherProjectId,
+      logicalProjectKey: "logical-b",
+      threadId,
+    });
+  });
+
   it("clears branch and worktree context when changing a draft thread project ref", () => {
     const store = useComposerDraftStore.getState();
     store.setProjectDraftThreadId(projectRef, draftId, {
diff --git a/apps/web/src/hooks/useHandleNewThread.ts b/apps/web/src/hooks/useHandleNewThread.ts
index 8dab93eb..fe6c01c2 100644
--- a/apps/web/src/hooks/useHandleNewThread.ts
+++ b/apps/web/src/hooks/useHandleNewThread.ts
@@ -47,6 +47,7 @@ function useNewThreadState() {
         branch?: string | null;
         worktreePath?: string | null;
         envMode?: DraftThreadEnvMode;
+        startupDraft?: boolean;
       },
     ): Promise<void> => {
       const {
@@ -96,6 +97,7 @@ function useNewThreadState() {
           await router.navigate({
             to: "/draft/$draftId",
             params: { draftId: storedDraftThread.draftId },
+            ...(options?.startupDraft ? { search: { startupDraft: "1" } } : {}),
           });
         })();
       }
@@ -122,6 +124,13 @@ function useNewThreadState() {
           ...(hasWorktreePathOption ? { worktreePath: options?.worktreePath ?? null } : {}),
           ...(hasEnvModeOption ? { envMode: options?.envMode } : {}),
         });
+        if (options?.startupDraft) {
+          return router.navigate({
+            to: "/draft/$draftId",
+            params: { draftId: currentRouteTarget.draftId },
+            search: { startupDraft: "1" },
+          });
+        }
         return Promise.resolve();
       }
 
@@ -142,6 +151,7 @@ function useNewThreadState() {
         await router.navigate({
           to: "/draft/$draftId",
           params: { draftId },
+          ...(options?.startupDraft ? { search: { startupDraft: "1" } } : {}),
         });
       })();
     },
diff --git a/apps/web/src/lib/chatThreadActions.test.ts b/apps/web/src/lib/chatThreadActions.test.ts
index 1c387e8f..2455c6b6 100644
--- a/apps/web/src/lib/chatThreadActions.test.ts
+++ b/apps/web/src/lib/chatThreadActions.test.ts
@@ -113,6 +113,31 @@ describe("chatThreadActions", () => {
     });
   });
 
+  it("marks startup new threads for startup draft presentation", async () => {
+    const handleNewThread = vi.fn<ChatThreadActionContext["handleNewThread"]>(async () => {});
+
+    const didStart = await startNewThreadFromContext(
+      createContext({
+        latestActivityThread: {
+          environmentId: ENVIRONMENT_ID,
+          projectId: PROJECT_ID,
+          branch: "feature/recent",
+          worktreePath: null,
+        },
+        handleNewThread,
+        startupDraft: true,
+      }),
+    );
+
+    expect(didStart).toBe(true);
+    expect(handleNewThread).toHaveBeenCalledWith(scopeProjectRef(ENVIRONMENT_ID, PROJECT_ID), {
+      branch: "feature/recent",
+      worktreePath: null,
+      envMode: "local",
+      startupDraft: true,
+    });
+  });
+
   it("starts a local thread with the configured default env mode", async () => {
     const handleNewThread = vi.fn<ChatThreadActionContext["handleNewThread"]>(async () => {});
 
diff --git a/apps/web/src/lib/chatThreadActions.ts b/apps/web/src/lib/chatThreadActions.ts
index 4199be86..613acd7b 100644
--- a/apps/web/src/lib/chatThreadActions.ts
+++ b/apps/web/src/lib/chatThreadActions.ts
@@ -20,6 +20,7 @@ interface NewThreadHandler {
       branch?: string | null;
       worktreePath?: string | null;
       envMode?: DraftThreadEnvMode;
+      startupDraft?: boolean;
     },
   ): Promise<void>;
 }
@@ -33,6 +34,7 @@ export interface ChatThreadActionContext {
   readonly defaultProjectRef: ScopedProjectRef | null;
   readonly defaultThreadEnvMode: DraftThreadEnvMode;
   readonly handleNewThread: NewThreadHandler;
+  readonly startupDraft?: boolean;
 }
 
 export function resolveThreadActionProjectRef(
@@ -65,6 +67,7 @@ function buildContextualThreadOptions(context: ChatThreadActionContext): NewThre
     worktreePath: threadContext?.worktreePath ?? context.activeDraftThread?.worktreePath ?? null,
     envMode:
       context.activeDraftThread?.envMode ?? (threadContext?.worktreePath ? "worktree" : "local"),
+    ...(context.startupDraft ? { startupDraft: true } : {}),
   };
 }
 
diff --git a/apps/web/src/routes/_chat.draft.$draftId.tsx b/apps/web/src/routes/_chat.draft.$draftId.tsx
index 77b9f18f..b82b4901 100644
--- a/apps/web/src/routes/_chat.draft.$draftId.tsx
+++ b/apps/web/src/routes/_chat.draft.$draftId.tsx
@@ -8,9 +8,18 @@ import { createThreadSelectorAcrossEnvironments } from "../storeSelectors";
 import { useStore } from "../store";
 import { buildThreadRouteParams } from "../threadRoutes";
 
+export interface DraftThreadRouteSearch {
+  startupDraft?: "1" | undefined;
+}
+
+function parseDraftThreadRouteSearch(search: Record<string, unknown>): DraftThreadRouteSearch {
+  return search.startupDraft === "1" ? { startupDraft: "1" } : {};
+}
+
 function DraftChatThreadRouteView() {
   const navigate = useNavigate();
   const { draftId: rawDraftId } = Route.useParams();
+  const routeSearch = Route.useSearch();
   const draftId = DraftId.make(rawDraftId);
   const draftSession = useComposerDraftStore((store) => store.getDraftSession(draftId));
   const serverThread = useStore(
@@ -76,11 +85,13 @@ function DraftChatThreadRouteView() {
         environmentId={draftSession.environmentId}
         threadId={draftSession.threadId}
         routeKind="draft"
+        startupDraft={routeSearch.startupDraft === "1"}
       />
     </SidebarInset>
   );
 }
 
 export const Route = createFileRoute("/_chat/draft/$draftId")({
+  validateSearch: (search) => parseDraftThreadRouteSearch(search),
   component: DraftChatThreadRouteView,
 });
diff --git a/apps/web/src/routes/_chat.index.tsx b/apps/web/src/routes/_chat.index.tsx
index 3e1ab0be..65a5bab9 100644
--- a/apps/web/src/routes/_chat.index.tsx
+++ b/apps/web/src/routes/_chat.index.tsx
@@ -48,6 +48,7 @@ function ChatIndexRouteView() {
         defaultEnvMode: appSettings.defaultThreadEnvMode,
       }),
       handleNewThread,
+      startupDraft: true,
     }).then((didStart) => {
       if (!didStart) {
         startInFlightRef.current = false;
-- 
2.50.1 (Apple Git-155)

