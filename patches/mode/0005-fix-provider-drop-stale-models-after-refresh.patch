From e9a45ef6bfb82474c8e7406261975e5db2d6c400 Mon Sep 17 00:00:00 2001
From: Devin AI <158243242+devin-ai-integration[bot]@users.noreply.github.com>
Date: Sun, 7 Jun 2026 02:04:53 +0000
Subject: [PATCH 05/10] fix(provider): drop stale models after refresh

---
 .../provider/Layers/ProviderRegistry.test.ts  | 45 +++++++++++++++++++
 .../src/provider/Layers/ProviderRegistry.ts   |  3 +-
 2 files changed, 46 insertions(+), 2 deletions(-)

diff --git a/apps/server/src/provider/Layers/ProviderRegistry.test.ts b/apps/server/src/provider/Layers/ProviderRegistry.test.ts
index 1b9abd65..55f995b2 100644
--- a/apps/server/src/provider/Layers/ProviderRegistry.test.ts
+++ b/apps/server/src/provider/Layers/ProviderRegistry.test.ts
@@ -594,6 +594,51 @@ it.layer(Layer.mergeAll(NodeServices.layer, ServerSettingsService.layerTest(), T
         ]);
       });
 
+      it("drops stale models when a successful refresh returns a non-empty replacement list", () => {
+        const previousProvider = {
+          instanceId: ProviderInstanceId.make("droid"),
+          driver: ProviderDriverKind.make("droid"),
+          status: "ready",
+          enabled: true,
+          installed: true,
+          auth: { status: "authenticated" },
+          checkedAt: "2026-04-14T00:00:00.000Z",
+          version: "1.0.0",
+          models: [
+            {
+              slug: "claude-opus-4.8",
+              name: "Opus 4.8",
+              isCustom: false,
+              capabilities: null,
+            },
+            {
+              slug: "claude-opus-4.8-fast",
+              name: "Opus 4.8 Fast",
+              isCustom: false,
+              capabilities: null,
+            },
+          ],
+          slashCommands: [],
+          skills: [],
+        } as const satisfies ServerProvider;
+        const refreshedProvider = {
+          ...previousProvider,
+          checkedAt: "2026-04-14T00:01:00.000Z",
+          models: [
+            {
+              slug: "claude-opus-4.8",
+              name: "Opus 4.8",
+              isCustom: false,
+              capabilities: null,
+            },
+          ],
+        } satisfies ServerProvider;
+
+        assert.deepStrictEqual(mergeProviderSnapshot(previousProvider, refreshedProvider).models, [
+          ...refreshedProvider.models,
+        ]);
+      });
+
       it("persists merged provider snapshots for the providers that were refreshed", () => {
         const previousProviders = [
           {
diff --git a/apps/server/src/provider/Layers/ProviderRegistry.ts b/apps/server/src/provider/Layers/ProviderRegistry.ts
index 22a120f0..a356331f 100644
--- a/apps/server/src/provider/Layers/ProviderRegistry.ts
+++ b/apps/server/src/provider/Layers/ProviderRegistry.ts
@@ -97,8 +97,7 @@ const mergeProviderModels = (
       capabilities: previousModel.capabilities,
     };
   });
-  const nextSlugs = new Set(nextModels.map((model) => model.slug));
-  return [...mergedModels, ...previousModels.filter((model) => !nextSlugs.has(model.slug))];
+  return mergedModels;
 };
 
 export const mergeProviderSnapshot = (
-- 
2.34.1

